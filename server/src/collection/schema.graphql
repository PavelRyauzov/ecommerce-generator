type Collection {
    id: ID!
    title: String!
    products: [Product!]!
    createdAt: String!
    updatedAt: String!
}

type Query {
    collections: [Collection!]!
    collection(id: ID!): Collection
}
