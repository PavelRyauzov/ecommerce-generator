type Image {
    id: ID!
    fileName: String!
    altText: String!
    product: Product!
    productId: Int!
    featuredBy: Product
    featuredById: Int
    createdAt: String!
    updatedAt: String!
}

type Query {
    images: [Image!]!
    image(id: ID!): Image
}

