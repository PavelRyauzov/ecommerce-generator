type Product {
    id: ID!
    availableForSale: Boolean!
    title: String!
    description: String!
    price: Int!
    featuredImage: Image
    images: [Image!]!
    characteristics: [Characteristic]
    createdAt: String!
    updatedAt: String!
}

type Query {
    products: [Product!]!
    product(id: ID!): Product
}